node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env.dev
.env.prod
.env.local
tsconfig.tsbuildinfo

credentials
credentials.json

# Firebase configuration file
google-services.json


ios
android

# macOS
.DS_Store

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*
goldenwheels-e2366-firebase-adminsdk-fbsvc-c46a24d712.json
# Any Firebase Admin SDK service-account key. These are SERVER credentials for FCM V1
# (upload via `eas credentials`), never client config and never GOOGLE_SERVICES_JSON.
*firebase-adminsdk*.json
